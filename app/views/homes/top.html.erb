<div class= "col-xs-3">
	<%= render 'public/items/sidebar', genres: @genres %>
</div>

<div class="col-xs-9">

	<section id="top">
		<p class= "top-text">
			ようこそ、NaganoCakeへ！<br>
			このサイトはケーキ販売のECサイトになります。<br>
			会員でない方も商品の閲覧はできますが、<br>
			購入には会員登録が必要になります。
		</p>
	</section>

	<section id="recommend-items">
		<h3>オススメ商品<h3>
			<% @random.each do |item| %>
			<div>
			    <%= link_to public_item_path(item) do %>
			      <%= attachment_image_tag(item, :image, class: "img-square",fallback: "img/no-image-mini.jpg",size:"300x300") %>
                <% end %>
            </div>
            <div>
                <%= item.name %>
            </div>
            <div>
                <%= (item.price.to_i*1.10).floor %>円
            </div>
            <% end %>
	</section>

	<div class="more-items">
		<%=link_to "もっと見る>", public_items_path %>
	</div>
</div>